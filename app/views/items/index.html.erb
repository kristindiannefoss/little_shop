
<h1>All Items</h1>

<h3>or shop by category:</h3>
<ul>
  <% @categories.each do |cat| %>
    <li><%= link_to cat.name, "/#{cat.name}" %></li>
  <% end %>
</ul>

<% @items.each do |item| %>
  <%= render partial: 'shared/item', locals: {item: item} %>
<% end %>
